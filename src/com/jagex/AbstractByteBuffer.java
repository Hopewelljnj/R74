package com.jagex;

public abstract class AbstractByteBuffer {

    static int revision;
    static classDc mouseWheelListener;
    static boolean fieldV = false;

    abstract void setBuffer(byte[] var1, byte DUMMY);

    abstract byte[] getBuffer(byte DUMMY);
}
